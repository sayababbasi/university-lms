# File: backend/apps/timetable/apps.py
# AppConfig for timetable (Developed by SAYAB)

from django.apps import AppConfig

class TimetableConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.timetable"
